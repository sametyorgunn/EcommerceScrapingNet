@using EntityLayer.Dto.ResponseDto
@using EntityLayer.Entity
@model List<TrendyolCategoryDto>



       @*  <input type="text" placeholder="Search.." class="form-control" id="myInput" onkeyup="filterFunction()">
        <select id="TrendyolCategoryName" size="5" name="TrendyolCategoryName" class="dropdown-content form-control">
            @foreach (var category in Model)
            {
                <option value="@category.name">@category.name</option>
            }
        </select>

        <script>
            function filterFunction() {
                const input = document.getElementById("myInput");
                const filter = input.value.toUpperCase();
                const select = document.getElementById("TrendyolCategoryName");
                const options = select.getElementsByTagName("option");

                for (let i = 0; i < options.length; i++) {
                    const txtValue = options[i].textContent || options[i].innerText;
                    if (txtValue.toUpperCase().indexOf(filter) > -1) {
                        options[i].style.display = "";
                    } else {
                        options[i].style.display = "none";
                    }
                }
            }
        </script>
         *@

<input name="TrendyolCategoryName" class="form-control" id="TrendyolCategoryName" type="text" />

